<% cache [cache_key_for_events, params], expires: 15.minutes do %>

    <!-- HEAD -->

    <%= stylesheet_link_tag 'suit-components-grid/index.css', 'suit-utils-size/index.css', 'suit-utils-position/index.css' %>
    <%= stylesheet_link_tag 'modules/Event', 'modules/AlertCreateEvent', 'modules/AvatarPhoto' %>
    <%= stylesheet_link_tag params[:controller] %>

    <%= include_gon %>
    <%= javascript_include_tag 'gmaps-builder-user' %>
    <%= javascript_include_tag params[:controller] %>


    <!-- MAIN -->

    <div class="col-lg-9 col-md-9 col-sm-8 col-xs-12">
      <div class="row">
        <div class="col-lg-12">

          <div id="main-content" class="Main ps-container">

            <%= render 'events_section', events: @events[:today], title: "Eventos acontecendo hoje em #{@events[:today][:city_name]}" unless @events[:today][:preview].blank? %>

            <%= render 'events_section', events: @events[:persona], title: 'Eventos indicados para você' unless @events[:persona][:preview].blank? %>

            <%= render 'events_section', events: @events[:neighborhood], title: "Eventos acontecendo no bairro #{@events[:neighborhood][:neighborhood_name]}" unless @events[:neighborhood][:preview].blank? %>

            <%= render 'events_section', events: @events[:fun], title: 'Eventos para se divertir' unless @events[:fun][:preview].blank? %>

            <%= render 'events_section', events: @events[:education], title: 'Aprender algo novo' unless @events[:education][:preview].blank? %>

            <%= render 'events_section', events: @events[:health], title: 'Cuidar da saude' unless @events[:health][:preview].blank? %>


          </div>
        </div>

      </div>
    </div>


    <!-- SIDEBAR -->

    <div class="col-lg-3 col-md-3 col-sm-4 hidden-xs u-margin-95-0">
      <%= render 'sidebar' %>
    </div>

<% end %>