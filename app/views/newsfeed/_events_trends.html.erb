<section class="Section" data-anchor="trends">
  <div class="Section-header">
    <h1 class="Section-title">Estão em alta</h1>
    <span class="Section-description"></span>
  </div>

  <div class="Grid Grid--withGutter">
    <%= render 'alert_none_events' %>

    <% if current_or_guest_user.guest? %>
        <%= render 'events_for_guest', events: @events[:trends] %>
    <% else %>
        <%= render 'event', events: @events[:trends] %>
    <% end %>

    <%= render 'alert_create_item', events: @events[:trends] %>
  </div>
</section>

