<section class="Section" data-anchor="fun">
  <div class="Section-header">
    <h1 class="Section-title">Se divertir</h1>
    <span class="Section-description">Eventos para você sair com os amigos, passear com a familia, pegar um sol, curtir um piquenique, namorar...</span>
  </div>

  <div class="Grid Grid--withGutter">
    <%= render 'alert_none_events' %>

    <% if current_or_guest_user.guest? %>
        <%= render 'events_for_guest', events: @events[:fun] %>
    <% else %>
        <%= render 'event', events: @events[:fun] %>
    <% end %>

    <%= render 'alert_create_item', events: @events[:fun] %>
  </div>
</section>
